name: CI

on:
  push:
    branches:
    - master

jobs:
  test:
    name: npm install, build and test
    runs-on: ubuntu-latest
    steps:
      - name: wait wurt
        run: |
          npm install
          npm run build
          npm test
          
          
